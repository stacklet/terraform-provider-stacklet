---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "stacklet_api_key Resource - terraform-provider-stacklet"
subcategory: ""
description: |-
  Manages an API key.
---

# stacklet_api_key (Resource)

Manages an API key.

## Example Usage

```terraform
# Create an API key with description
resource "stacklet_api_key" "example" {
  description = "API key for automated scripts"
}

# Create an API key with expiration
resource "stacklet_api_key" "expiring" {
  description = "Temporary API key for testing"
  expires_at  = "2026-12-31T23:59:59+00:00"
}

# Get the key secret
output "api_key_secret" {
  value     = stacklet_api_key.example.secret
  sensitive = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `description` (String) The description of the API key.
- `expires_at` (String) The timestamp when the API key expires (RFC3339 format). Changing expiration creates a new key.

### Read-Only

- `id` (String) The GraphQL Node ID of the API key.
- `identity` (String) The identity of the API key.
- `revoked_at` (String) The timestamp when the API key was revoked (RFC3339 format).
- `secret` (String, Sensitive) The API key secret.

## Import

Import is supported using the following syntax:

In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

```terraform
import {
  to = stacklet_api_key.example
  id = "$identity"
}
```

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import stacklet_api_key.example $identity
```
